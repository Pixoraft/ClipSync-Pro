services:
  - type: web
    name: clipsync-pro
    env: node
    plan: free
    buildCommand: npm install && node build.js
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /